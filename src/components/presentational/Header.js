import React from 'react';

export const Header = ({ mylist }) => (
  <div style={{height: '80px'}}>
    <img src={'/Netflix_Logo_RGB.png'} alt={'netflix_logo'} style={{height: '90%'}} />
  </div>
);
